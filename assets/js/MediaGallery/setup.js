(function($) {

	// App.assets = new App.Assets();
	// App.assets.fetch({add: true, success: function() {
	// 	App.assets = new App.CommentsView({collection:App.comments}).render().el;
	// 	App.commentInputView = new App.CommentInputView({model:App.comments}).render().el;	
	// }});

}(jQuery));
